// Basic Flutter widget test for FlowLedger

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App starts correctly', (WidgetTester tester) async {
    // Basic smoke test - will be expanded with proper tests later
    expect(true, isTrue);
  });
}
